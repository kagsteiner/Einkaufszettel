{
  "name": "einkaufszettel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.20.2"
  },
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env scripts/dev.ts",
    "start": "tsx --env-file-if-exists=.env src/server/main.ts",
    "build": "npm run typecheck && tsx scripts/build-client.ts --production",
    "build:client": "tsx scripts/build-client.ts",
    "test": "tsx --test --test-concurrency=1 tests/*.test.ts",
    "test:coverage": "tsx --experimental-test-coverage --test --test-concurrency=1 tests/*.test.ts",
    "test:browser": "npm run build:client && playwright test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check --write .",
    "icons:generate": "tsx scripts/generate-product-icons.ts",
    "check": "npm run typecheck && npm test && npm run lint && npm run test:browser && npm run build"
  },
  "dependencies": {
    "argon2": "^0.44.0",
    "openai": "^6.48.0",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4",
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
